#ifndef __TUXNDSHELLS_H_
#define __TUXNDSHELLS_H_

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#if __linux__
#define createDirectory(dirname)
#define createFile(filename)
#define showDirectory(string)
#endif

#endif